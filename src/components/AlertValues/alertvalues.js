import React from "react";


let Alert = "alert";

const SetAlert = (x) => {
    Alert = x;
}

let ShowAlert = false;

const SetShowAlert = (y) => {
    ShowAlert = y;
}

export {Alert, SetAlert, ShowAlert, SetShowAlert};








